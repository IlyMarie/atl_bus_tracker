<p id="notice"><%= notice %></p>

<h1>Buses, at your Service!</h1>

<h3 class="text-muted">Current Location</h3>

<p class="text-muted">
  <%= @location.full_street_address %> <small><%= link_to fa_icon('pencil', text: "Edit"), edit_location_path(@location) %></small>

</p>

<h3>The closest buses are...</h3>
<% @buses.each do |bus| %>
  <div class='well'>
    <strong>Route: </strong><%= bus["ROUTE"] %> <%= bus["DIRECTION"] %> <br>
    <strong>Bus Number: </strong><%= bus["VEHICLE"] %> <br>
    <strong>Next Stop: </strong><%= bus["TIMEPOINT"] %>
  </div>
<% end %>

<% if @buses.empty? %>
  <h3>
    Not really that close. Consider other options like ride a bike, uber or call a friend, or <%= link_to 'change your location', edit_location_path(@location) %>.
  </h3>
  <% end %>